{% extends 'base.html.twig' %}

{% block title %}Sede index{% endblock %}

{% block stylesheets %}

    <link href="{{ asset('css/styles.css') }}" rel="stylesheet" type="text/css"/>

{% endblock %}

{% block body %}

    <div class="titulo">
               <a href={{path('app_homepage')}} > <h1>Sistema de gestión académica IS</h1></a>1>Sistema de gestión académica IS</h1></a>1>Sistema de gestión académica IS</h1></a>1>Sistema de gestión académica IS</h1></a>1>Sistema de gestión académica IS</h1></a>1>Sistema de gestión académica IS</h1></a>1>Sistema de gestión académica IS</h1></a>1>Sistema de gestión académica IS</h1></a> <a href={{path('app_homepage')}} > <></ah1>Sistema de gestión académica IS</h1></a>
    </div>

   <div class="barra">
        <a class="boton-home" href={{path('app_homepage')}} > <img src="{{asset('media/casita.png')}}"> </a>
        <a class="btn btn-lg" href={{path('app_institutos')}} >Institutos</a>
        <a class="btn btn-lg" href={{path('app_preinsc')}} >Preinscripción</a>
        <a class="btn btn-lg" href={{path('app_plataforma')}} >Acceso a la plataforma</a>
    </div>
    <h1>Sede index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Instituto</th>
                <th>Direccion</th>
                <th>Telefono</th>
                <th>Mail</th>
                <th>Url_campus</th>
                <th>Url_instituto</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for sede in sedes %}
            <tr>
                <td>{{ sede.id }}</td>
                <td>{{ sede.nombre }}</td>
                <td>{{ sede.instituto }}</td>
                <td>{{ sede.direccion }}</td>
                <td>{{ sede.telefono }}</td>
                <td>{{ sede.mail }}</td>
                <td>{{ sede.urlCampus }}</td>
                <td>{{ sede.urlInstituto }}</td>
                <td>
                    <a href="{{ path('app_sede_show', {'id': sede.id}) }}">show</a>
                    <a href="{{ path('app_sede_edit', {'id': sede.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="8">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_sede_new') }}">Create new</a>
{% endblock %}
