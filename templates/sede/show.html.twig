{% extends 'base.html.twig' %}

{% block title %}Sede{% endblock %}

{% block stylesheets %}

    <link href="{{ asset('css/styles.css') }}" rel="stylesheet" type="text/css"/>

{% endblock %}

{% block body %}

    <div class="titulo">
 <h1>Sistema de gestión académica IS</h1>
    </div>
     <div class="barra">
        <a class="boton-home" href={{path('app_homepage')}} > <img src="{{asset('media/casita.png')}}"> </a>
        <a class="btn btn-lg" href={{path('app_institutos')}} >Institutos</a>
        <a class="btn btn-lg" href={{path('app_preinsc')}} >Preinscripción</a>
        <a class="btn btn-lg" href={{path('app_plataforma')}} >Acceso a la plataforma</a>
    </div>
    <h1>Sede</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ sede.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ sede.nombre }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ sede.direccion }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ sede.telefono }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ sede.mail }}</td>
            </tr>
            <tr>
                <th>Url_campus</th>
                <td>{{ sede.urlCampus }}</td>
            </tr>
            <tr>
                <th>Url_instituto</th>
                <td>{{ sede.urlInstituto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_sede_index') }}">back to list</a>

    <a href="{{ path('app_sede_edit', {'id': sede.id}) }}">edit</a>

    {{ include('sede/_delete_form.html.twig') }}
{% endblock %}
