{% extends 'base.html.twig' %}

{% block title %}
    FIST
{% endblock %}

{% block stylesheets %}

    <link href="{{ asset('css/styles.css') }}" rel="stylesheet" type="text/css"/>

{% endblock %}

{% block body %}

    <div class="titulo">
        <h1>Sistema de gestión académica IS</h1>
    </div>

    <div class="barra">
        <a class="boton-home" href={{path('app_homepage')}} > <img src="{{asset('media/casita.png')}}"> </a>
        <a class="btn btn-lg" href={{path('app_institutos')}} >Institutos</a>
        <a class="btn btn-lg" href={{path('app_preinsc')}} >Preinscripción</a>
        <a class="btn btn-lg" href={{path('app_plataforma')}} >Acceso a la plataforma</a>
    </div>
    <h1>OfertaEducativa</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ oferta_educativa.id }}</td>
            </tr>
            <tr>
                <th>Ciclo</th>
                <td>{{ oferta_educativa.ciclo }}</td>
            </tr>
            <tr>
                <th>Vacantes</th>
                <td>{{ oferta_educativa.vacantes }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_oferta_educativa_index') }}">back to list</a>

    <a href="{{ path('app_oferta_educativa_edit', {'id': oferta_educativa.id}) }}">edit</a>

    {{ include('oferta_educativa/_delete_form.html.twig') }}
{% endblock %}
