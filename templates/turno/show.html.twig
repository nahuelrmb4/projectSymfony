{% extends 'base.html.twig' %}

{% block title %}
    FIST
{% endblock %}

{% block stylesheets %}

    <link href="{{ asset('css/styles.css') }}" rel="stylesheet" type="text/css"/>

{% endblock %}

{% block body %}

    <div class="titulo">
           <a href={{path('app_homepage')}} > <h1>Sistema de gestión académica IS</h1></a>
    </div>

    <div class="barra">
        <a class="boton-home" href={{path('app_homepage')}} > <img src="{{asset('media/casita.png')}}"> </a>
        <a class="btn btn-lg" href={{path('app_institutos')}} >Institutos</a>
        <a class="btn btn-lg" href={{path('app_preinsc')}} >Preinscripción</a>
        <a class="btn btn-lg" href={{path('app_plataforma')}} >Acceso a la plataforma</a>
    </div>
    <h1>Turno</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ turno.id }}</td>
            </tr>
            <tr>
                <th>Horario</th>
                <td>{{ turno.horario }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_turno_index') }}">back to list</a>

    <a href="{{ path('app_turno_edit', {'id': turno.id}) }}">edit</a>

    {{ include('turno/_delete_form.html.twig') }}
{% endblock %}
