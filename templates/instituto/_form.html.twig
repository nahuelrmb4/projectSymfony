<div class="container mt-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            {{ form_start(form, {'attr': {'class': 'form-horizontal ml'}}) }}
                <div class="form-group">
                    {{ form_widget(form, {'attr': {'class': 'form-control'}}) }}
                </div>
               <center> <button class="btn btn-primary" type="submit">{{ button_label|default('Guardar') }}</button></center>
            {{ form_end(form) }}
        </div>
    </div>
</div>
