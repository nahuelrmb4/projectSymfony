{% extends 'base.html.twig' %}

{% block title %}Instituto{% endblock %}

{% block stylesheets %}

    <link href="{{ asset('css/styles.css') }}" rel="stylesheet" type="text/css"/>

{% endblock %}

{% block body %}

    <div class="titulo">
            <h1>Sistema de gestión académica IS</h1>
    </div>

    <div class="barra">
        <a class="boton-home" href={{path('app_instituto_index')}}> <img src="{{asset('media/casita.png')}}"> </a>
        <a class="btn btn-lg" href={{path('app_instituto_index')}}>Institutos</a>
        <a class="btn btn-lg" href={{path('app_instituto_index')}}>Preinscripción</a>
        <a class="btn btn-lg" href={{path('app_instituto_index')}}>Acceso a la plataforma</a>
    </div>
    <h1>Instituto</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ instituto.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ instituto.nombre }}</td>
            </tr>
            <tr>
                <th>Direccion</th>
                <td>{{ instituto.direccion }}</td>
            </tr>
            <tr>
                <th>Telefono</th>
                <td>{{ instituto.telefono }}</td>
            </tr>
            <tr>
                <th>Mail</th>
                <td>{{ instituto.mail }}</td>
            </tr>
            <tr>
                <th>Url_campus</th>
                <td>{{ instituto.urlCampus }}</td>
            </tr>
            <tr>
                <th>Url_instituto</th>
                <td>{{ instituto.urlInstituto }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_instituto_index') }}">back to list</a>

    <a href="{{ path('app_instituto_edit', {'id': instituto.id}) }}">edit</a>

    {{ include('instituto/_delete_form.html.twig') }}
{% endblock %}
